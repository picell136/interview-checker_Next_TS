import { q } from "@/data/questions/helpers";
import type { QuizQuestion } from "@/types/quiz";

export const typescriptQuestions: QuizQuestion[] = [
  q("ts-1", "Чем TypeScript отличается от JavaScript?", "Это надмножество JS со статической типизацией, которое компилируется в JS", ["Это отдельная VM вместо V8", "TypeScript выполняется в браузере без компиляции", "В TypeScript нет объектов"], "TS добавляет типы и проверки на этапе компиляции. В рантайме остаётся обычный JavaScript."),
  q("ts-2", "В чём разница между any и unknown?", "unknown безопаснее: сначала нужно сузить тип, any отключает проверки", ["any нельзя присвоить никуда", "unknown совместим только со string", "Разницы нет"], "any можно использовать как угодно. unknown требует проверки или assertion."),
  q("ts-3", "Когда тип never появляется в TypeScript?", "Когда функция не возвращает управление или ветка невозможна", ["Когда значение может быть null", "Когда тип выведен как any", "Когда используется void"], "never — нет значений: throw, бесконечный цикл, исчерпывающий switch. void — «ничего полезного»."),
  q("ts-4", "Чем interface отличается от type?", "interface можно расширять и сливать декларации, type гибче для union/tuple", ["type нельзя использовать для объектов", "interface нельзя extend", "type существует только в .d.ts"], "interface удобен для публичных контрактов. type — для union, mapped и условных типов."),
  q("ts-5", "Что делает флаг strictNullChecks?", "Запрещает случайно использовать null/undefined без проверки", ["Удаляет null из JavaScript в рантайме", "Делает все поля обязательными", "Включает только noImplicitAny"], "null и undefined не входят в каждый тип автоматически. Пишут string | null и сужают тип."),
  q("ts-6", "Для чего нужны дженерики?", "Чтобы писать переиспользуемый код с сохранением конкретных типов", ["Чтобы отключить проверку типов", "Чтобы заменить enum", "Чтобы ускорить компиляцию"], "identity<T>(value: T): T сохраняет тип аргумента. Без дженерика пришлось бы any."),
  q("ts-7", "Что возвращает keyof для интерфейса { id: number; name: string }?", '"id" | "name"', ["string", "number", "Array<string>"], "keyof берёт объединение ключей. Часто с mapped types: { [K in keyof T]: ... }."),
  q("ts-8", "Что делает утилита Partial<T>?", "Делает все свойства T необязательными", ["Удаляет null из T", "Оставляет только методы T", "Превращает T в readonly"], "Partial<T> = { [K in keyof T]?: T[K] }. Противоположность — Required<T>."),
  q("ts-9", "Чем Pick<T, K> отличается от Omit<T, K>?", "Pick оставляет ключи K, Omit исключает их", ["Omit оставляет ключи, Pick удаляет", "Оба удаляют методы", "Pick работает только с union"], "Pick<User, \"id\" | \"email\"> — выбранные поля. Omit<User, \"password\"> — все кроме указанных."),
  q("ts-10", "Что такое type narrowing?", "Сужение union-типа проверками typeof, in, instanceof или type guard", ["Удаление типов при компиляции", "Превращение interface в any", "Переименование алиаса"], "После if (typeof x === \"string\") x — string. Guard: function isCat(x): x is Cat."),
  q("ts-11", "Зачем нужен оператор satisfies?", "Проверить, что значение соответствует типу, не расширяя его до этого типа", ["Заменить as const", "Отключить infer", "Создать runtime-валидацию"], "satisfies ловит ошибки структуры, но сохраняет литеральные типы полей."),
  q("ts-12", "Чем enum отличается от as const объекта?", "enum — отдельная runtime-сущность, as const остаётся обычным объектом с литеральными типами", ["as const создаёт IIFE в бандле всегда", "enum нельзя использовать как тип", "as const работает только с числами"], "Числовые enum эмитят объект в JS. as const + typeof + keyof даёт похожие литералы без enum."),
  q("ts-13", "Что делает tsc?", "Компилирует TypeScript и проверяет типы", ["Запускает Node REPL", "Это пакетный менеджер", "Минификатор CSS"], "tsc --noEmit только проверяет. tsc --watch пересобирает при изменениях."),
  q("ts-14", "Для чего tsconfig.json?", "Настройки компилятора: target, module, strict, paths", ["Список npm-скриптов", "Конфиг ESLint по умолчанию", "Манифест браузера"], "extends позволяет делить конфиг. include/exclude задают, какие файлы входят в проект."),
  q("ts-15", "Что включает флаг strict?", "Набор строгих проверок: noImplicitAny, strictNullChecks и другие", ["Только запрет any", "Только ESNext", "Отключение source maps"], "Это пакет опций. Можно включать их по отдельности, но strict — разумный дефолт."),
  q("ts-16", "Что делает noImplicitAny?", "Требует тип, если компилятор не может его вывести", ["Запрещает unknown", "Вставляет any во все параметры", "Отключает infer"], "function f(x) без типа параметра при этом флаге — ошибка. Явный any всё ещё возможен."),
  q("ts-17", "Чем void отличается от undefined в типах функций?", "void значит «результат не используют», функция всё же может вернуть undefined", ["void — это never", "undefined запрещён как return", "Разницы нет"], "() => void можно реализовать функцией, которая возвращает значение — вызывающий его игнорирует."),
  q("ts-18", "Что такое union-тип?", "Тип «одно из»: string | number", ["Тип «и то и другое сразу»", "Только для enum", "Синоним any"], "Сужают проверками. Intersection A & B — объект, удовлетворяющий обоим."),
  q("ts-19", "Что такое intersection-тип A & B?", "Значение должно соответствовать и A, и B", ["Это union", "Это never всегда", "Только для примитивов"], "Для объектов поля сливаются. string & number сводится к never."),
  q("ts-20", "Что делает typeof в TypeScript (в типе)?", "Берёт тип значения: type T = typeof config", ["Это runtime typeof из JS в том же месте", "Удаляет объект", "Создаёт экземпляр класса"], "В выражении typeof — JS. В аннотации типа — запрос типа. Часто keyof typeof obj."),
  q("ts-21", "Что такое условный тип?", "T extends U ? X : Y", ["if в рантайме", "Только для классов", "Синоним enum"], "Распределяются по union, если T — «голый» параметр. infer вытаскивает кусок типа."),
  q("ts-22", "Для чего infer в условных типах?", "Чтобы вытащить вложенный тип, например аргумент или return", ["Чтобы отключить strict", "Это runtime-оператор", "Замена any"], "type Head<T> = T extends [infer H, ...unknown[]] ? H : never."),
  q("ts-23", "Что такое mapped type?", "Тип, который пробегает по ключам: { [K in keyof T]: ... }", ["Цикл for в JS", "Декоратор класса", "Только JSON Schema"], "Так устроены Partial, Readonly, Pick. Можно добавлять as для переименования ключей (TS 4.1+)."),
  q("ts-24", "Что делает Readonly<T>?", "Делает все поля только для чтения", ["Замораживает объект в рантайме как Object.freeze всегда", "Удаляет методы", "Делает поля optional"], "Это тип. Чтобы запретить мутации в JS, нужен Object.freeze или иммутабельная практика."),
  q("ts-25", "Что делает Exclude<T, U>?", "Убирает из union T те члены, которые присваиваемы U", ["Удаляет ключи объекта", "Синоним Omit", "Оставляет только null"], "Exclude<\"a\" | \"b\" | \"c\", \"a\"> → \"b\" | \"c\". Extract — наоборот, пересечение."),
  q("ts-26", "Что делает NonNullable<T>?", "Убирает null и undefined из T", ["Делает T = any", "Добавляет ?", "Превращает в never"], "NonNullable<string | null> → string. Удобно после строгих null-проверок."),
  q("ts-27", "Что возвращает ReturnType<typeof fn>?", "Тип значения, которое возвращает функция fn", ["Тип первого аргумента", "typeof самого fn", "Promise всегда"], "Для async функции ReturnType будет Promise<...>. Параметры — Parameters<typeof fn>."),
  q("ts-28", "Что такое ограничение дженерика T extends U?", "T обязан быть подтипом U", ["T становится any", "Запрещает U", "Это runtime instanceof"], "function getId<T extends { id: string }>(x: T) — внутри можно читать x.id."),
  q("ts-29", "Что делает as const?", "Литералы сужаются максимально, поля становятся readonly", ["Удаляет типы", "Включает enum", "Это assertion в never"], "['a', 'b'] as const → readonly [\"a\", \"b\"], а не string[]."),
  q("ts-30", "Чем type assertion as Type отличается от удовлетворения типу?", "as говорит компилятору «считай так», даже если это опасно", ["as проверяет данные в runtime", "as запрещён для any", "Разницы с satisfies нет"], "document.querySelector(...) as HTMLInputElement не проверяет, что элемент — input."),
  q("ts-31", "Что такое пользовательский type guard?", "Функция с предикатом x is Type", ["Декоратор параметра", "ESLint-правило", "Только instanceof"], "if (isUser(data)) внутри блока data имеет тип User. Это сужение, не валидация Zod сама по себе."),
  q("ts-32", "Что такое дискриминированное объединение?", "Union объектов с общим литеральным полем-меткой", ["Intersection всех вариантов", "enum без runtime", "any | never"], "type Shape = { kind: \"circle\"; r: number } | { kind: \"square\"; s: number }. switch (shape.kind) сужает поля."),
  q("ts-33", "Чем public, private и protected отличаются в TS?", "Видимость для тайпчекера: везде / только класс / класс+наследники", ["private скрывает поле в runtime всегда без #", "protected виден снаружи инстанса", "public запрещает new"], "Эмитируются обычные поля, если не использовать #private. JS private fields (#) — настоящая изоляция."),
  q("ts-34", "Что делает readonly у поля класса?", "Запрещает присваивание после инициализации (на уровне типов)", ["Делает getter в runtime", "Запрещает чтение", "Синоним const объекта"], "Можно задать в конструкторе. Для глубокой неизменности readonly не рекурсивен сам по себе."),
  q("ts-35", "Что такое abstract class?", "Класс, который нельзя инстанциировать, только наследовать", ["Интерфейс в runtime", "Синоним namespace", "Декоратор метода"], "abstract методы обязаны реализовать наследники. В JS останется обычная функция-конструктор."),
  q("ts-36", "Для чего .d.ts файлы?", "Описывают типы существующего JS без его переписывания", ["Хранят исходники TS", "Это source maps", "Конфиг Jest"], "@types/node подкладывает декларации. declare module \"foo\" описывает пакет без типов."),
  q("ts-37", "Что делает declare?", "Говорит: сущность есть в runtime, типы описываю я", ["Создаёт переменную", "Импортирует ESM", "Включает experimentalDecorators"], "declare const VERSION: string — не эмитится в JS, предполагается глобаль."),
  q("ts-38", "Чем namespace отличается от ES-модуля?", "namespace — старая группировка TS, модули — import/export файлов", ["namespace работает только в браузере", "Модули нельзя в Node", "Разницы нет"], "В новом коде предпочитают ESM. namespace ещё встречается в типах библиотек."),
  q("ts-39", "Чем числовой enum отличается от строкового?", "Числовой может быть обратным маппингом в объекте, строковый — нет", ["Строковый enum не существует в JS", "Числовой нельзя использовать как тип", "Они не эмитятся"], "const enum вырезается в литералы при компиляции (с оговорками isolatedModules)."),
  q("ts-40", "Как типизировать props в React с TS?", "type Props = { title: string }; function Comp({ title }: Props)", ["Только PropTypes", "React.FC обязателен всегда", "props: any единственный способ"], "React.FC добавляет children неявно в старых версиях — многие команды его избегают."),
  q("ts-41", "Как типизировать useRef для DOM-узла?", "useRef<HTMLInputElement | null>(null)", ["useRef<number>(0) для input", "useRef<string>(\"div\")", "Рефы нельзя типизировать"], "После монтирования ref.current — элемент или null. Для мутабельного бокса без DOM: useRef(0)."),
  q("ts-42", "Что делает esModuleInterop?", "Сглаживает default import из CommonJS-модулей", ["Отключает ESM", "Включает JSX", "Удаляет require"], "Позволяет import React from \"react\" там, где модуль экспортирует module.exports = ..."),
  q("ts-43", "Для чего skipLibCheck?", "Не проверять типы в .d.ts зависимостей", ["Пропускать ваши исходники", "Отключать emit", "Игнорировать tsconfig"], "Ускоряет сборку и обходит ошибки в чужих декларациях. Свои файлы всё равно проверяются."),
  q("ts-44", "Что такое module augmentation?", "Расширение уже существующего модуля или интерфейса", ["Удаление export", "Замена bundler", "Синоним namespace merge только для enum"], "declare module \"next\" { interface ... } или interface Array<T> { ... } в глобале."),
  q("ts-45", "Чем Zod отличается от типов TypeScript?", "Zod валидирует данные в runtime, TS стирается при компиляции", ["Zod заменяет tsc", "Типы TS проверяют API-ответы сами", "Разницы нет"], "Типы не защитят от плохого JSON с сервера. Схема Zod может вывести тип через z.infer."),
  q("ts-46", "Что делает оператор in при сужении?", "Проверяет наличие ключа и сужает union объектов", ["Это цикл for...in в типе", "Удаляет свойство", "Работает только с массивами"], "if (\"x\" in obj) помогает, когда варианты union имеют разные поля."),
  q("ts-47", "Что такое utility Awaited<T>?", "Разворачивает Promise, как await", ["Делает функцию async", "Синоним ReturnType", "Удаляет then"], "Awaited<Promise<Promise<string>>> → string. Удобно для обёрток над промисами."),
  q("ts-48", "Зачем paths и baseUrl в tsconfig?", "Алиасы импортов вроде @/components", ["Это URL сайта", "Настройка Next Image", "Только для Jest"], "Сборщик (Next, Vite) должен понимать те же алиасы, иначе runtime не найдёт модуль."),
  q("ts-49", "Что такое excess property check?", "Лишние поля в объектном литерале, присваиваемом явно типу, — ошибка", ["Проверка JSON Schema", "Запрет optional", "Это skipLibCheck"], "const u: User = { id: 1, extra: true } не пройдёт. Через переменную промежуточного типа проверка слабее."),
  q("ts-50", "Что делает isolatedModules?", "Каждый файл должен быть транспилируем отдельно (как Babel/SWC)", ["Запрещает import", "Включает project references", "Отключает JSX"], "const enum и некоторые re-export плохо сочетаются с isolatedModules. Next.js обычно включает этот режим."),
  q("ts-51", "Что делает utility Record<K, T>?", "Строит объект с ключами K и значениями T", ["Это массив T", "Удаляет ключи", "Синоним Map в runtime"], "Record<string, number> — словарь. Record<\"a\" | \"b\", User> — оба ключа обязательны."),
  q("ts-52", "Чем unknown[] отличается от any[]?", "Элементы unknown нельзя использовать без сужения, any[] — можно как угодно", ["Разницы нет", "unknown[] запрещён", "any[] не итерируется"], "map по unknown[] заставит обработать элемент. any[] отключает проверку всей цепочки."),
  q("ts-53", "Что такое tuple в TypeScript?", "[string, number] — массив фиксированной длины с типами по индексу", ["Это Set", "Только readonly never", "Синоним Array<string | number> без порядка"], "const pair: [string, number] = [\"age\", 1]. Optional элементы и rest в кортежах поддерживаются."),
  q("ts-54", "Для чего template literal types?", "Строить строковые типы из кусков: `on${Capitalize<K>}`", ["Это runtime шаблоны JS", "Только для enum", "Замена string"], "Помогают типизировать имена событий и CSS-классы. Infer вытаскивает части строки."),
  q("ts-55", "Что делает Parameters<typeof fn>?", "Кортеж типов аргументов функции", ["Тип this", "Только первый аргумент", "ReturnType"], "Удобно прокинуть те же аргументы в обёртку. ConstructorParameters — для constructor."),
  q("ts-56", "Чем implements отличается от extends у класса?", "implements обещает форму, extends наследует реализацию", ["implements копирует методы в runtime обязательно", "extends нельзя с interface", "Разницы нет"], "class A implements B проверяет наличие полей. Не добавляет код сам по себе."),
  q("ts-57", "Что такое definite assignment assertion name!: string?", "Говорит компилятору: поле точно присвоят, хотя не в конструкторе явно", ["Делает поле optional", "Runtime проверка", "Синоним ?"], "Опасно, если поле реально останется undefined. Лучше инициализировать или сужать."),
  q("ts-58", "Зачем ts-node / tsx?", "Запуск TS без предварительного tsc emit в файл", ["Замена типов React", "Линтер", "Сборщик CSS"], "Для скриптов и тестов. Прод часто собирают в JS. tsx быстрее на esbuild."),
  q("ts-59", "Что делает noUncheckedIndexedAccess?", "obj[key] и arr[i] становятся T | undefined", ["Запрещает массивы", "Включает any", "Это skipLibCheck"], "Честнее отражает JS: элемента может не быть. Приходится проверять перед использованием."),
  q("ts-60", "Как типизировать JSON.parse?", "Результат unknown (или схема), не any", ["JSON.parse всегда User", "Это generic обязательный в стандарте", "Тип выводится из строки в runtime движком"], "JSON.parse возвращает any в lib.dom — лучше обёртка + Zod. Слепой as User скрывает баги."),
  q("ts-61", "Что такое branded types?", "Отличить UserId от string, хотя в JS это строка", ["Создаёт класс в emit", "Замена uuid библиотеки", "Это enum"], "type UserId = string & { readonly __brand: \"UserId\" }. Нельзя случайно передать обычную string."),
  q("ts-62", "Чем overload signatures отличаются от implementation?", "Сверху список вызываемых форм, снизу одна реализация с широким типом", ["Каждый overload эмитится отдельной функцией", "Overload работает в runtime", "Implementation должна быть уже любой"], "function f(x: string): number; function f(x: number): string; function f(x: string | number) { ... }."),
  q("ts-63", "Что делает keyof typeof для const-объекта?", "Объединение ключей объекта как литералов", ["number", "string всегда без литералов", "Array"], "Вместе с (typeof obj)[keyof typeof obj] получают union значений."),
  q("ts-64", "Для чего declaration: true в tsc?", "Генерировать .d.ts рядом с JS для библиотеки", ["Включает source maps только", "Запрещает JS emit", "Это ESLint"], "Пакеты на npm отдают types в package.json. declarationMap помогает jump-to-source."),
  q("ts-65", "Что такое recursive type?", "Тип, который ссылается на себя, например дерево Json", ["Бесконечный any", "Запрещён в TS", "Только классы"], "type Json = string | number | Json[] | { [k: string]: Json }. Нужны границы, иначе ошибка глубины."),
  q("ts-66", "Чем this-тип в методе полезен?", "Описывает, каким должен быть объект, у которого вызвали метод", ["Меняет runtime this стрелки", "Синоним never", "Только для static"], "function fluent(this: Builder) { return this }. Стрелки this не типизируют так."),
  q("ts-67", "Что делает утилита Extract<T, U>?", "Оставляет из union T только членов, совместимых с U", ["Extract удаляет файлы", "Разница только в runtime", "Синоним Exclude"], "Extract<string | Date, Function> → never. Часто фильтруют union по форме объекта."),
  q("ts-68", "Как типизировать process.env в Node?", "Расширить NodeJS.ProcessEnv в .d.ts", ["process.env всегда Record без unknown", "Только NEXT_PUBLIC", "Типы подтянутся из .env автоматически всегда"], "interface ProcessEnv { DATABASE_URL: string }. Значения всё равно строки | undefined без валидации."),
  q("ts-69", "Что такое variance у дженериков функций при strictFunctionTypes?", "Параметры функций проверяются контравариантно безопаснее", ["Это skipLibCheck", "Отключает callbacks", "Только для классов"], "(x: Animal) => void нельзя подставить туда, где ждут (x: Dog) => void, если метод пишет в x по-собачьи."),
  q("ts-70", "Зачем satisfies вместе с as const на конфиге?", "Проверить ключи схемы и сохранить узкие литералы значений", ["Удаляет as const", "Это runtime freeze", "Замена enum обязательно"], "const routes = { home: \"/\" } as const satisfies Record<string, string>; автокомплит путей остаётся точным."),
];
